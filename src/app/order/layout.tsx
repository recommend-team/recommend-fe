export const metadata = {
  title: "Order - Recommend",
  description: "Your order details",
};

export default function OrderLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
