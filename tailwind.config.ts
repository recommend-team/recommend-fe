export default {
  theme: {
    extend: {
      fontFamily: {
        champ: ['BBH Hegarty', 'sans-serif'],
        dm: ['DM Sans', 'sans-serif'],

     extend: {
  animation: {
    float: "float 3s ease-in-out infinite",
    "float-slow": "float 5s ease-in-out infinite",
  },
  keyframes: {
    float: {
      "0%, 100%": { transform: "translateY(0px)" },
      "50%": { transform: "translateY(-10px)" },
    },
  },
  fontFamily: {
    champ: ["BBH Hegarty", "sans-serif"],
    dm: ["DM Sans", "sans-serif"],
  },
},
      },
    },

  },
};